# Required libraries for compiling the engine:
    libsdl2-dev libsdl2-ttf-dev libsdl2-mixer-dev

# Install on Ubuntu:
    sudo apt install libsdl2-dev libsdl2-ttf-dev libsdl2-mixer-dev

# Install on Arch:
    sudo pacman -S sdl2 sdl2_ttf sdl2_mixer
